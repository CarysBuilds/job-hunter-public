# Platform Policy

Job Hunter is a user-operated local tool.

- Users must log in with their own accounts.
- Crawls are manually triggered.
- Default delays are intentionally conservative.
- The public build does not include automatic message sending.
- Platform adapters may break when websites change; users should verify results before acting.

If a platform blocks or challenges access, stop the crawl and resolve it in the official Chrome page.
